// initialize the msettings library for styles etc. here

/* Currently the settings library doesn't contain any styles or views,
   registering is therefore disabled.
#include "mlibrary.h"

M_LIBRARY
*/

